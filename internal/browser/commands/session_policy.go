package commands

const (
	defaultSessionFlagUsage = "Browser session name; defaults to default. Omit --session for normal workflows, and do not invent a name from the task or website."
	defaultSessionNameUsage = "Session name; defaults to default. Omit --name for normal workflows, and do not invent a name from the task or website."
)
