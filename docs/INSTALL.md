# Install

Download the release archive for your operating system and CPU architecture, extract it, and place `jira` and `confluence` on your `PATH`.

Verify the install:

```bash
jira version --json
confluence version --json
```

The tools support linux, darwin, and windows on amd64 and arm64.
